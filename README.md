# DevRadar
Automated data pipeline that tracks demand for tech skills in job postings — scraping + LLM-based extraction (Groq) + dbt transformations on DuckDB, orchestrated with GitHub Actions and visualized in Streamlit.
